/**
 * Preload do Electron (roda no processo do renderer, antes da página).
 * Mantido como módulo vazio enquanto o jogo não precisar de IPC;
 * é o arquivo apontado em webPreferences.preload.
 */
export {}
